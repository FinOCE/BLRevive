import Muzzle from '../Muzzle'

export default class extends Muzzle {
  constructor() {
    super({
      name: 'Vulcan Notched Flash Breaker', // MISSING DATA
      damage: -2,
      recoil: -0.15
    })
  }
}
